# Empirical strategy

Our sample comprises 12,480 firm-year observations spanning 2010--2020. The
estimated treatment coefficient is 0.125 (p < 0.05), with `Outcome` as the
dependent variable. We estimate a difference-in-differences model using
`Treatment` and `Post`, following the design discussion in \citep{smith2022}.
