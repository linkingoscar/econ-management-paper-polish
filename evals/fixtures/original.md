# Empirical strategy

The sample contains 12,480 firm-year observations from 2010--2020. The treatment
coefficient is 0.125 (p < 0.05), and the outcome is defined as `Outcome`.
We estimate a difference-in-differences model with `Treatment` and `Post` and
cite the design discussion in \citep{smith2022}.
